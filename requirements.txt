transformers>=4.40.0
torch>=2.0.0
# app.py contains dozens of CSS selectors that depend directly on Streamlit's
# internal DOM details (data-testid, st-emotion-cache classes), which are not
# an official/stable API (see README "Known fragilities"). Pinned to a
# narrow version range instead of a broad >= range for this reason — verify
# the UI manually (or by screenshotting with .claude/skills/run-duygu-analizi
# driver.mjs) before bumping to a newer version.
streamlit>=1.61.0,<1.62.0
pandas>=2.0.0
sentencepiece>=0.2.0
pytest>=8.0.0
openpyxl>=3.1.0
accelerate>=1.0.0
fpdf2>=2.8.7
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
slowapi>=0.1.9
