# Python
venv/
__pycache__/
*.pyc
.pytest_cache/

# Fine-tuned model weights — large (~1.1GB each), not suitable for git.
# Model versioning is a separate matter (HF Hub / DVC) — see README.
duygu_finetuned_v3/
duygu_finetuned_v4_full/
duygu_finetuned_v5_full/
_egitim_gecici_*/

# Node / frontend build output and dependencies
node_modules/
frontend/dist/
frontend/.env.local

# Screenshots produced by the /run skill for verification — not permanent
# project content, just transient test output.
.claude/skills/run-duygu-analizi/screenshots/

# Local data / feedback database
geri_bildirimler.db
geri_bildirimler.csv
egitim_duzeltmeleri.csv

# Streamlit / editor / OS
.streamlit/secrets.toml
.DS_Store

# The design skill's large, seeded canvas output file (~2.4MB, also embeds
# the editor code) — the source .dc.html/canvas.json files (the actual
# design content we wrote) are tracked; this is just the compiled output.
tasarim/yorum-pusulasi-marka-yonleri.html
